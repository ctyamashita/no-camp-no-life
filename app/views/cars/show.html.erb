<div class="container">
  <div class="row mt-5">
    <% if @my_car %>
      <%= render 'update_form' %>
    <% else %>
      <div class="card">
        <img class="show-img me-5" src=<%= @car.photo.attached? ? cl_image_path(@car.photo.key) : '/assets/not-available.jpg' %> />
        <div class="col">
          <div class="row">
            <div class="col-12 col-md-10 ms-4">
              <div>
                <h1><strong><%= @car.car_model %></strong></h1>
                <p><%= @car.description %></p>
                <div class="d-flex">
                  <p class="border-bottom pb-3 me-5"><i class="fas fa-users"></i><strong> <%= @car.capacity %> people</strong></p>
                  <p class="border-bottom pb-3"><i class="far fa-money-bill-alt"></i> <strong>¥<%= @car.price_per_day %>/day</strong></p>
                </div>
              </div>
            </div>
          </div>
        <%= render 'bookings/form' %>
      </div>
    <% end %>
</div>
