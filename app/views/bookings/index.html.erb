<div class="container mt-5 col-12">
  <div class="d-flex align-items-center mb-5">
  <h1>My page</h1>
    <%= link_to 'My cars', my_cars_cars_path, class: "btn btn-primario ms-3"  %>
    <%= link_to "My Bookings", bookings_path, class: "btn btn-primario ms-3" %>
    <%= link_to 'Booking requests', my_cars_bookings_path, class: "btn btn-primario ms-3" %>
  </div>
  <hr>

  <div class="mt-5 col-12">
    <h2 class="mt-3">Bookings</h2>
    <% @bookings.each do |booking| %>
      <%= render 'booking_card', booking: booking %>
    <% end %>
  </div>
</div>
